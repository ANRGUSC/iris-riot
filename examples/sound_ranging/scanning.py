# from subprocess import run
from subprocess import call

port_usb = '0'
# threshold = '45'

call(['ls'])

call(['sudo make term PORT=ttyUSB' + port_usb])

# call(['sudo make term PORT=ttyUSB' + port_usb])


# call(['range_rx' + threshold])


# python script file



# range_rx

# frequency update - every so often according to arg


# print(data)









